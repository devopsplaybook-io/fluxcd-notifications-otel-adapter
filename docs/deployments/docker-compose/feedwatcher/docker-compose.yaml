# https://hub.docker.com/r/otel/opentelemetry-collector-contrib/tags#!
version: "3"

services:
  otel:
    image: didierhoarau/fluxcd-notifications-otel-adapter
    ports:
      - 8080:8080
    volumes:
      - ./data:/data
