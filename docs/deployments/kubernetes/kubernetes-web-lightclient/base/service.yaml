apiVersion: v1
kind: Service
metadata:
  name: fluxcd-notifications-otel-adapter
spec:
  type: NodePort
  ports:
    - name: tcp
      port: 8080
      targetPort: 8080
  selector:
    app: fluxcd-notifications-otel-adapter
